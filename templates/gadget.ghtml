{{define "content"}}
  <div id="content">
    {{.Gadget.Name}}
    {{range $location, $devices := .Gadget.Status}}
      <div>
        {{range $key, $device := $devices}}
          {{ template "device" $device}}
        {{end}}
      </div>
    {{end}}
  </div>
{{end}}

