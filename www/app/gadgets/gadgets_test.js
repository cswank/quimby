'use strict';

describe('quimby.gadgets module', function() {

  beforeEach(module('quimby.gadgets'));

  describe('quimby controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ctrl = $controller('GadgetsCtrl');
      expect(ctrl).toBeDefined();
    }));

  });
});
